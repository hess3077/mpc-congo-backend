{% extends 'master.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                {% if flash.message %}
                <div class="alert alert-danger alert-dismissable" style="margin-top: 25px">
                    <button aria-hidden="true" data-dismiss="alert" class="close" type="button">×</button>
                    {{ flash.message }}
                </div>
                {% endif %}
                <div class="login-panel panel panel-default" style="margin-top:40px">
                    <div class="panel-body">
                        <form role="form" method="post" action="{{baseUrl()}}/login">
                            <input type="hidden" name="redirect" value="{{ flash.redirect ? : redirect}}">
                            <fieldset>
                                <div class="form-group">
                                    <input class="form-control" placeholder="Email de connexion" name="email" type="email" autofocus value="{{ flash.email }}">
                                </div>
                                <div class="form-group">
                                    <input class="form-control" placeholder="Mot de passe" name="password" type="password" value="">
                                </div>
                                <div class="checkbox">
                                    <label>
                                        <input name="remember" type="checkbox" value="Remember Me" {% if flash.remember %}checked{% endif %}>Rester conneté
                                    </label>
                                </div>

                                <div class="form-group">
                                    <div class="col-xs-6 text-center">
                                        <button type="submit" class="btn btn-md btn-success btn-block">connexion</button>
                                    </div>
                                    <div class="col-xs-6 text-center">
                                    </div>
                                </div>
                            </fieldset>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}